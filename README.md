# OauthTest

discord oauth2 test

## setup

npm install

login.component.ts:

set `client_id`

server.js:

set `client_id`
set `client_secret`

## Development server

Run `ng serve` frontend

`node server.js` for backend
